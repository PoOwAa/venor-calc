{
  "name": "venor-calc",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "generate:items": "node scripts/download-and-create-items.mjs",
    "generate:shops": "node scripts/download-and-create-shops.mjs",
    "generate:icons": "node scripts/download-icons.mjs",
    "generate:all": "npm run generate:items && npm run generate:shops && npm run generate:icons"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.112.4",
    "@vitejs/plugin-react": "latest",
    "react": "latest",
    "react-dom": "latest",
    "tesseract.js": "^7.0.0",
    "typescript": "latest",
    "vite": "latest"
  },
  "devDependencies": {
    "@types/react": "latest",
    "@types/react-dom": "latest",
    "supabase": "^2.115.0"
  }
}